import 'dart:io';

import 'package:project3/project3.dart' as project3;

void main()
{
  // print("enter your name");
  // String ? name=stdin.readLineSync();
  // print("$name");
  // print("enter your age");
  // int ? age=int.parse(stdin.readLineSync()!);
  // stdout.write("my age is $age my name is $name");
  // String ? name2="ronal";
  // print("${name2.length}");
  String ? name3="qwerty";
  print("${name3.length}");
}
